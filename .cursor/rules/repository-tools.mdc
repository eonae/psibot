---
alwaysApply: true
---
- В репозитории используется yarn@4 и lerna
- для сборки проекта всегда используй `lerna run build`
- имей ввиду, что --watch не подхватывает изменения в пакетах. Поэтому собираем всё. Это не долго, т. к. есть кеширование
- Миграции для drizzle ты генерируешь только через скрипт yarn mig:generate
- Нельзя менять версии зависимостей без прямого разрешения!
