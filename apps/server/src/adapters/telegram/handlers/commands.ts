import type { Bot, Context } from 'grammy';

export function registerCommands(bot: Bot): void {
  // /start command
  bot.command('start', async (ctx: Context) => {
    await ctx.reply(
      `👋 Привет! Я бот для создания протоколов психологических сессий.\n\n` +
        `Вы можете отправить мне аудиофайл или ссылку на файл в Яндекс.Диск или Google Drive.\n\n` +
        `Для начала работы отправьте аудиофайл или ссылку.`,
    );
  });

  // /help command
  bot.command('help', async (ctx: Context) => {
    await ctx.reply(
      `📖 Помощь\n\n` +
        `Я могу обработать аудиофайлы и создать протокол сессии.\n\n` +
        `Поддерживаемые форматы: MP3, M4A, OGG, WAV, WEBM\n\n` +
        `Максимальный размер файла: без ограничений (если файл загружается через ссылку)\n\n` +
        `Отправьте аудиофайл или ссылку на файл в Яндекс.Диск/Google Drive для начала обработки.`,
    );
  });
}
