{
  "name": "@speechkit/yandex",
  "version": "0.1.0",
  "private": true,
  "license": "ISC",
  "main": "dist/index.js",
  "scripts": {
    "lint": "run -T eslint 'src/**/*.ts'",
    "lint:fix": "yarn lint --fix",
    "test": "run -T jest --passWithNoTests --detectOpenHandles",
    "build": "yarn clean && run -T tsc",
    "clean": "run -T rimraf dist/ node_modules/"
  },
  "dependencies": {
    "@rsdk/common": "6.0.0-next.14",
    "@rsdk/logging": "6.0.0-next.14",
    "@speechkit/core": "workspace:*",
    "axios": "^1.7.9",
    "fluent-ffmpeg": "^2.1.3",
    "form-data": "^4.0.4",
    "reflect-metadata": "^0.1.14"
  }
}
